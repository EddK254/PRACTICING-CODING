cd: cd
    It changes the working directory of shell.

    Current directory is changed to DIR. Default DIR is value of
    HOME shell variable.

    CDPATH variable defines search path for directory containing
    DIR. Colon(:)separates alternative directory names.
    Null directory is also current directory. If DIR begins
    with slash(/), then CDPATH is not used.

    Exit Status:
    Returns 0 if directory changed, and if $PWD is set successfully when
    -P is used; otherwise non-zero.

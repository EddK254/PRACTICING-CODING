  unset: unset
    Shell variables and functions, their values and attributes are unset

    Corresponding variable or function for each NAME removed.
    Without options, unset first tries to unset a variable, if it fails,

    Exit Status:
    Unless invalid option is given or NAME is read-only, Return success
